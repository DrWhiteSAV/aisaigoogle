# Project Rules

- **No Full Caps**: Never use full uppercase text in the UI (e.g., avoid `uppercase` class in Tailwind). All text should use standard sentence case or title case (e.g., "Ваши питомцы" instead of "ВАШИ ПИТОМЦЫ"). 
- **Typography**: Maintain a hand-drawn, cozy feel using standard casing.
